import React from 'react';

function Subtitle({subtitle}) {
  return (
    <div className='creator-subtitle'>
        <span className='following'>{subtitle}</span>
    </div>
  )
}

export default Subtitle;